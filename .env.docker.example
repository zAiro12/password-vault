# Docker Compose Environment Variables
# Copy this file to .env and fill in your values
# ⚠️ SECURITY: All passwords and keys MUST be changed from defaults!

# Database Configuration
DB_ROOT_PASSWORD=your_secure_root_password_here
DB_NAME=password_vault
DB_USER=vault_user
DB_PASSWORD=your_secure_db_password_here
DB_PORT=3306

# Backend Configuration
BACKEND_PORT=3000

# Admin User (created during first migration)
ADMIN_DEFAULT_USERNAME=lucaairoldi
ADMIN_DEFAULT_PASSWORD=your_secure_admin_password_here
ADMIN_DEFAULT_EMAIL=lucaairoldi92@gmail.com

# Security Keys
# ⚠️ CRITICAL: Generate new secure keys - do NOT use placeholder values!
# Generate JWT_SECRET: openssl rand -base64 32
JWT_SECRET=your_generated_jwt_secret_here

# Generate ENCRYPTION_KEY: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your_generated_64_char_hex_key_here

# ⚠️ WARNING: These keys must remain constant across deployments!
# Changing JWT_SECRET will invalidate all existing user sessions
# Changing ENCRYPTION_KEY will make all encrypted data unreadable
