# Frontend Configuration

# API Base URL (used in production)
# In development, Vite proxy is used (configured in vite.config.js)
VITE_API_BASE_URL=http://localhost:3000

# Application Environment
VITE_ENV=development
